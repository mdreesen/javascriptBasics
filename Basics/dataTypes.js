// what are the dataTypes in javascript

const types = ['numbers', 'strings', 'boolean(true or false)', 'null', 'undefined', 'symbol']

const dataTypes = (data) => {
        if (types === true) {
            console.log(`That is a correct DataType ${data}`)
        } else {
            console.log('Please try again');
    }
};

dataTypes('numbers');